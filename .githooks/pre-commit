#!/usr/bin/env bash
set -euo pipefail
root="$(git rev-parse --show-toplevel)"
base_ref="$(git -C "$root" config --get codex-warp.preflight-base || printf '%s' origin/main)"
exec bash "$root/scripts/ci-preflight.sh" --base "$base_ref"
